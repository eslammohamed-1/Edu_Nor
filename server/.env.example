DATABASE_URL="file:./dev.db"
# جذر بنك الأسئلة (اختياري): مسار مطلق، أو مسار نسبي من جذر المستودع — الافتراضي data/question-bank
# QUESTION_BANK_ROOT=data/question-bank
JWT_SECRET="dev-only-change-to-long-random-string-in-production"
JWT_ACCESS_EXPIRES_SEC=900
JWT_REFRESH_EXPIRES_DAYS=7
COOKIE_SECURE=false
PORT=3001

SUPER_ADMIN_EMAIL=superadmin@edunor.local
SUPER_ADMIN_PASSWORD=EduNorSuper2026!

# Comma-separated origins; * allows any (dev only)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# مسار مجلد CSV (مثل مسجل الطلاب). إذا تركت فارغة يُستخدم ../data/csv بالنسبة لمجلد server/
# CSV_DATA_DIR=
