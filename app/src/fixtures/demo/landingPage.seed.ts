/** محتوى الصفحة الرئيسية التسويقي — يُحمَّل من CSV المُولَّد */
import landing from '@/fixtures/demo-catalog/generated/landing.json';

export const landingFeatures = landing.features;
export const landingStages = landing.stages;
export const landingMarketingStats = landing.stats;
